version: '3.8'

services:
  # Import services from docker/docker-compose.yml
  postgres:
    extends:
      file: ./docker/docker-compose.yml
      service: postgres
    env_file:
      - ./docker/.env

  redis:
    extends:
      file: ./docker/docker-compose.yml
      service: redis
    env_file:
      - ./docker/.env

  pgadmin:
    extends:
      file: ./docker/docker-compose.yml
      service: pgadmin
    env_file:
      - ./docker/.env

  # Application service
  # Uncomment when ready to containerize the application
  # app:
  #   build:
  #     context: .
  #     dockerfile: docker/Dockerfile
  #   container_name: kavzitrader-app
  #   restart: unless-stopped
  #   volumes:
  #     - .:/app
  #   environment:
  #     - POSTGRES_HOST=postgres
  #     - POSTGRES_PORT=5432
  #     - POSTGRES_USER=${POSTGRES_USER}
  #     - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
  #     - POSTGRES_DB=${POSTGRES_DB}
  #     - REDIS_HOST=redis
  #     - REDIS_PORT=6379
  #     - BINANCE_API_KEY=${BINANCE_API_KEY}
  #     - BINANCE_API_SECRET=${BINANCE_API_SECRET}
  #     - BINANCE_TESTNET=${BINANCE_TESTNET}
  #   ports:
  #     - "8000:8000"
  #   depends_on:
  #     - postgres
  #     - redis
  #   command: ["python", "-m", "src.cli.main"]

volumes:
  postgres_data:
  redis_data:
  pgadmin_data:
