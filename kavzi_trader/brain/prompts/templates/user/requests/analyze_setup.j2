Validate this potential setup on {{ market_snapshot.timeframe }} {{ market_snapshot.symbol }}.
Symbol tier: {{ symbol_tier | default("TIER_2") }} | Min confidence: {{ tier_min_confidence | default("0.75") }}
Leverage: {{ futures_leverage | default(3) }}x (liquidation ~{{ (100 / (futures_leverage | default(3))) | round(1) }}% from entry)

STEP 1 — Review algorithm confluence signals for BOTH directions:
{% include "user/context/algorithm_confluence.j2" %}
Your confluence_score base MUST match the algorithm score for your chosen direction (LONG base={{ algorithm_confluence_long.score }}/8, SHORT base={{ algorithm_confluence_short.score }}/8).
You must add 0-3 discretionary points (order flow + price structure) to produce your final 0-11 confluence_score. The max achievable is 8 (algo base) + 3 (discretionary) = 11, matching the schema cap.

STEP 2 — Analyze market data:
{% include "user/context/market_snapshot.j2" %}

STEP 3 — Assess order flow:
{% include "user/context/order_flow.j2" %}

STEP 4 — Consider external market sentiment:
{% include "user/context/sentiment.j2" %}

Now perform your analysis following the framework in your instructions.
