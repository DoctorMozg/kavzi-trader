Algorithm confluence (7 binary signals):
LONG ({{ algorithm_confluence_long.score }}/7): EMA:{{ "Y" if algorithm_confluence_long.ema_alignment else "N" }} RSI:{{ "Y" if algorithm_confluence_long.rsi_favorable else "N" }} VOL:{{ "Y" if algorithm_confluence_long.volume_above_average else "N" }} BB:{{ "Y" if algorithm_confluence_long.price_at_bollinger else "N" }} FUND:{{ "Y" if algorithm_confluence_long.funding_favorable else "N" }} OI:{{ "Y" if algorithm_confluence_long.oi_supports_direction else "N" }} SPIKE:{{ "Y" if algorithm_confluence_long.volume_spike else "N" }}
SHORT ({{ algorithm_confluence_short.score }}/7): EMA:{{ "Y" if algorithm_confluence_short.ema_alignment else "N" }} RSI:{{ "Y" if algorithm_confluence_short.rsi_favorable else "N" }} VOL:{{ "Y" if algorithm_confluence_short.volume_above_average else "N" }} BB:{{ "Y" if algorithm_confluence_short.price_at_bollinger else "N" }} FUND:{{ "Y" if algorithm_confluence_short.funding_favorable else "N" }} OI:{{ "Y" if algorithm_confluence_short.oi_supports_direction else "N" }} SPIKE:{{ "Y" if algorithm_confluence_short.volume_spike else "N" }}
Detected trend direction: {{ detected_side }}
